name: ktlint

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  ktlint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Run ktlint
        run: ./gradlew ktlintCheck